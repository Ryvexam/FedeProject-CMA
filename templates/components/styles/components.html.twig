{# components/styles/components.html.twig #}
{% macro component_styles() %}
    <style>
        .group-pill {
            cursor: pointer;
            outline: none;
        }

        .group-pill:focus {
            outline: none;
        }
    </style>
{% endmacro %}