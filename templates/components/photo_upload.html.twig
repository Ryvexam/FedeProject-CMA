{# components/photo_upload.html.twig #}
{% macro photo_upload(form_widget) %}
    <div class="space-y-2">
        {{ form_label(form_widget, 'Photo', {'label_attr': {'class': 'block text-sm font-medium text-gray-700 mb-1'}}) }}
        <div class="relative">
            <label class="block">
                <span class="sr-only">Choose photo</span>
                {{ form_widget(form_widget, {
                    'attr': {
                        'class': 'hidden',
                        'data-photo-input': 'true'
                    }
                }) }}
                <div class="relative">
                    <div class="inline-flex items-center px-4 py-3 border-2 border-green-200 rounded-lg text-gray-700 bg-white hover:bg-green-50 cursor-pointer transition-all duration-300">
                        <svg class="w-5 h-5 mr-2 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-8l-4-4m0 0L8 8m4-4v12"/>
                        </svg>
                        <span class="text-sm" data-photo-label>Choose a photo...</span>
                    </div>
                    <div class="mt-2 text-sm text-gray-500" id="photo-name"></div>
                </div>
            </label>
        </div>
        {{ form_errors(form_widget) }}
    </div>
{% endmacro %}